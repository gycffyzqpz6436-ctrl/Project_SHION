model_id: mistralai/Ministral-3-8B-Instruct-2512-BF16
model_revision: f6fae9795746f63c9be8344932f01275f3c63734
model_path: D:/AI/Project_SHION/models/mistral/ministral-3-8b-instruct-2512-bf16
model_class: AutoModelForImageTextToText
tokenizer_fix_mistral_regex: true

quantization:
  load_in_4bit: true
  type: nf4
  double_quantization: true
  compute_dtype: bfloat16

lora:
  rank: 8
  alpha: 16
  dropout: 0.10
  bias: none
  target_modules_regex: '^model\.language_model\.layers\.\d+\.self_attn\.(q_proj|k_proj|v_proj|o_proj)$'

training:
  learning_rate: 0.00005
  epochs: 3
  per_device_train_batch_size: 1
  gradient_accumulation_steps: 8
  optimizer: paged_adamw_8bit
  scheduler: cosine
  warmup_ratio: 0.10
  weight_decay: 0.01
  max_grad_norm: 1.0
  gradient_checkpointing: true
  gradient_checkpointing_use_reentrant: false
  use_cache: false
  bf16: true
  fp16: false
  tf32: true
  attention_implementation: sdpa
  save_strategy: epoch
  save_total_limit: 3
  logging_steps: 5
