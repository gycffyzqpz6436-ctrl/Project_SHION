"use strict";
const $=(id)=>document.getElementById(id);
const ui={messages:$("messages"),form:$("composer"),input:$("message"),send:$("send"),model:$("model"),mode:$("mode"),reset:$("reset"),status:$("status"),dot:$("status-dot"),note:$("model-note"),info:$("model-info")};
const sessionId=crypto.randomUUID();let busy=false;
function escapeHtml(value){return value.replace(/[&<>"']/g,(c)=>({"&":"&amp;","<":"&lt;",">":"&gt;","\"":"&quot;","'":"&#39;"}[c]));}
function markdown(value){let safe=escapeHtml(value),blocks=[];safe=safe.replace(/```([^\n]*)\n([\s\S]*?)```/g,(_,lang,code)=>{blocks.push(`<pre><code data-language="${lang.trim()}">${code}</code></pre>`);return `\u0000${blocks.length-1}\u0000`;});safe=safe.replace(/`([^`\n]+)`/g,"<code>$1</code>").replace(/\*\*([^*]+)\*\*/g,"<strong>$1</strong>").replace(/\n/g,"<br>");return safe.replace(/\u0000(\d+)\u0000/g,(_,i)=>blocks[Number(i)]);}
function addMessage(role,text,temporary=false){document.querySelector(".welcome")?.remove();const row=document.createElement("div");row.className=`message ${role}${temporary?" thinking":""}`;const bubble=document.createElement("div");bubble.className="bubble";bubble.innerHTML=markdown(text);row.appendChild(bubble);ui.messages.appendChild(row);ui.messages.scrollTop=ui.messages.scrollHeight;return row;}
function setStatus(state){ui.status.textContent=state;ui.dot.className="";const key=state.toLowerCase();if(key==="ready")ui.dot.classList.add("ready");else if(key==="generating"||key==="loading model")ui.dot.classList.add("generating");else if(key==="error")ui.dot.classList.add("error");const ready=state==="Ready"&&!busy;ui.input.disabled=!ready;ui.send.disabled=!ready;ui.model.disabled=!ready;}
function populateModels(models){if(ui.model.options.length)return;for(const item of models){const option=new Option(`${item.display_name} · ${item.provenance}`,item.alias);option.disabled=!item.available;option.title=item.unavailable_reason||item.modification_type;ui.model.add(option);}}
async function poll(){try{const response=await fetch("/api/status",{cache:"no-store"});const data=await response.json();populateModels(data.models||[]);setStatus(data.state);if(data.model_alias){ui.model.value=data.model_alias;ui.note.textContent=`${data.provenance} · ${data.parameter_scale} · ${data.gpu_memory_allocated_mib} MiB`;ui.info.textContent=`${data.display_name} | ${data.repo_id} | Parent: ${data.parent_model} | Base origin: ${data.base_origin||"not specified"} | ${data.modification_type} | ${data.quantization} | Mode: ${ui.mode.value}`;}}catch{setStatus("Error");ui.note.textContent="Local server unavailable";}}
async function send(event){event.preventDefault();const message=ui.input.value.trim();if(!message||busy)return;busy=true;ui.input.value="";ui.input.style.height="auto";addMessage("user",message);const waiting=addMessage("assistant","応答を生成しています…",true);setStatus("Generating");try{const response=await fetch("/api/chat",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({session_id:sessionId,mode:ui.mode.value,message})});const data=await response.json();waiting.remove();if(!response.ok)throw new Error(data.error||"応答に失敗しました。");addMessage("assistant",data.response);}catch(error){waiting.remove();addMessage("assistant",error.message||"モデルの応答生成に失敗しました。");}finally{busy=false;await poll();ui.input.focus();}}
async function newChat(){if(busy)return;await fetch("/api/reset",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({session_id:sessionId})});ui.messages.innerHTML='<div class="welcome"><p class="eyebrow">NEW CONVERSATION</p><h2>新しい対話を始めましょう。</h2><p>以前の会話履歴はメモリから削除されました。</p></div>';}
ui.form.addEventListener("submit",send);ui.input.addEventListener("keydown",(event)=>{if(event.key==="Enter"&&!event.shiftKey){event.preventDefault();ui.form.requestSubmit();}});ui.input.addEventListener("input",()=>{ui.input.style.height="auto";ui.input.style.height=`${Math.min(ui.input.scrollHeight,180)}px`;});ui.reset.addEventListener("click",newChat);ui.mode.addEventListener("change",async()=>{await newChat();await poll();});
ui.model.addEventListener("change",async()=>{if(busy)return;setStatus("Loading model");const response=await fetch("/api/model",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({session_id:sessionId,model_alias:ui.model.value})});if(!response.ok){const data=await response.json();addMessage("assistant",data.error||"Model switching failed.");}await poll();});
poll();setInterval(poll,1500);
